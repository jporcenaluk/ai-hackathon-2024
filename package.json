{
    "name": "hackathon",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "frontend": "npm start --prefix hackathon",
        "backend": "uvicorn backend.main:app --reload",
        "dev": "concurrently \"npm run frontend\" \"npm run backend\""
    },
    "dependencies": {
        "concurrently": "^6.5.1"
    }
}
